# Промпты для Gemini в Google Slides

> Набор промптов для наполнения презентации «Подходы к архитектуре фронтенда».
> По одному промпту на слайд (14 слайдов). Язык контента — русский.
> Пустые слайды создаёте вручную; каждый промпт оформляет **текущий** (уже открытый) слайд.
> Перед началом один раз задайте единый стиль (см. блок «Установка стиля»).

---

## Установка стиля (выполнить один раз перед слайдами)

```
Для всей этой презентации на тему «Подходы к архитектуре фронтенда: от простого к сложному»
используй единый стиль: минималистичный, технический, деловой. Светлая тема, один акцентный цвет,
крупные заголовки, моноширинный шрифт для кода и структур папок. Без лишних украшений.
Все тексты — на русском языке. Соотношение сторон 16:9.
Везде, где есть блоки «Плюсы» и «Минусы», оформляй их единообразно: заголовок «Плюсы» —
зелёным цветом с иконкой плюса (✅), заголовок «Минусы» — красным цветом с иконкой минуса (⛔).
```

---

## Слайд 1 — Титульный

```
Оформи текущий слайд как титульный.
Заголовок: «Подходы к архитектуре фронтенда».
Подзаголовок: «От простого к сложному: карта решений 2026».
Внизу — место для имени докладчика и даты.
Визуал: крупный заголовок по центру, лаконичный фон, без графиков.
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```

## Слайд 2 — Зачем нужна архитектура

```
Оформи текущий слайд на тему «Зачем нужна архитектура».
Подзаголовок: «Что происходит в проектах без неё».
Размести список проблем:
— высокая стоимость изменений: одну правку приходится вносить в нескольких местах, и она ломает другое;
— замедление разработки: каждая новая фича добавляется дольше предыдущей по мере роста кодовой базы;
— тяжёлый онбординг: без явной структуры новому человеку и AI-агенту долго входить в проект;
— страх рефакторинга: неясно, что с чем зацеплено, поэтому рабочий код боятся трогать.
Итог одной строкой: «Архитектура — это набор правил, который поглощает сложность по мере роста».
Визуал: список с иконками-предупреждениями, выделенный итоговый тезис внизу.
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```

## Слайд 3 — Цели архитектуры (Low Coupling / High Cohesion)

```
Оформи текущий слайд на тему «Чего мы хотим от архитектуры».
Вводная строка: «К чему мы стремимся, выстраивая архитектуру (из принципов GRASP — ключевые два)».
Размести два пункта:
— Low Coupling — слабая зависимость между модулями: изменение в одном не ломает остальные;
— High Cohesion — сильная связность внутри модуля: связанный код лежит вместе.
Итог: «Все методологии далее — это способы добиться low coupling + high cohesion».
Визуал: матрица 2×2 (оси Coupling и Cohesion); отметь квадрант «low coupling + high cohesion»
как идеальный, «high coupling + low cohesion» — как худший. Оставь место под эту схему справа.
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```

## Слайд 4 — Карта подходов: 3 группы

```
Оформи текущий слайд на тему «Карта подходов: три группы».
Размести три колонки:
1. Возникшие на фронтенде — Atomic Design, Feature-based (популярный пример — Bulletproof React), Feature-Sliced Design;
2. Универсальные архитектурные подходы, адаптированные под фронтенд — Layered, Clean, Hexagonal, Onion, а также стратегический Domain-Driven Design;
3. Охватывающие несколько приложений — Monorepo, Micro-Frontends.
Сноска: «Деление условное и служит для удобства — это не строгая классификация».
Визуал: три вертикальные колонки-карточки с названиями подходов.
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```

## Слайд 5 — Уровень 0. Flat structure

```
Оформи текущий слайд на тему «Уровень 0. Flat structure».
Размести структуру папок (моноширинный шрифт):
src/
  components/
  pages/
  hooks/
  utils/
  api.js
Описание: «Группировка по типу файла, без правил зависимостей».
Плюсы: минимальный порог входа и мгновенный старт; технологически независим (подходит любому фреймворку).
Минусы: нет правил зависимостей (границ); низкая масштабируемость — с ростом дорожают изменения и теряется навигация; применим только к маленьким проектам.
Заголовок «Плюсы» — зелёным с иконкой плюса (✅), «Минусы» — красным с иконкой минуса (⛔).
Когда: прототип, MVP, до 10 экранов.
Визуал: блок с деревом папок слева, плюсы/минусы — двумя колонками справа.
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```

## Слайд 6 — Уровень 1. Модульная организация

```
Оформи текущий слайд на тему «Уровень 1. Модульная организация».
Подзаголовок: «Feature-based → Domain-oriented».
Размести структуру папок (моноширинный):
src/
  modules/
    billing/
      index.ts   (публичный API)
      ui/ model/ api/
    orders/
  shared/
Описание: «Режем код на модули. Две вариации: feature-based (по сценариям) и
domain-oriented (по доменам). Отличие — в строгости границ».
Плюсы: невысокий порог входа в большинстве вариаций; технологически независим (принцип организации, не привязан к фреймворку); хорошая масштабируемость — растёт от feature- к domain-варианту.
Минусы: границы держатся на договорённостях — без проверки линтером со временем размываются; много похожих, но разных вариаций — нет единого канона, риск разнобоя в команде.
Заголовок «Плюсы» — зелёным с иконкой плюса (✅), «Минусы» — красным с иконкой минуса (⛔).
Когда: от небольшой команды до крупного продукта с доменами.
Визуал: дерево папок слева, плюсы/минусы справа.
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```

## Слайд 7 — Уровень 2. Atomic Design

```
Оформи текущий слайд на тему «Уровень 2. Atomic Design».
Размести структуру (моноширинный):
atoms → molecules → organisms → templates → pages
Описание: «Самостоятельная методология (Brad Frost): организует весь интерфейс
по уровню сложности — от примитива до целой страницы».
Плюсы: технологически независим (применим в любом фреймворке); консистентный переиспользуемый UI и единый язык с дизайном; невысокий порог входа — интуитивная иерархия от простого к сложному.
Минусы: область применения ограничена UI — не задаёт организацию состояния, данных и бизнес-логики; семантика слоёв оторвана от предметной области — «молекулы» и «организмы» слабо соотносятся с бизнес-требованиями, деление ощущается искусственным.
Заголовок «Плюсы» — зелёным с иконкой плюса (✅), «Минусы» — красным с иконкой минуса (⛔).
Когда: UI-центричные продукты и дизайн-системы.
Визуал: горизонтальная диаграмма-цепочка из пяти уровней с нарастающей сложностью.
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```

## Слайд 8 — Уровень 3. Feature-Sliced Design (FSD)

```
Оформи текущий слайд на тему «Уровень 3. Feature-Sliced Design (FSD)».
Размести структуру слоёв (моноширинный, сверху вниз):
app → pages → widgets → features → entities → shared
(внутри слайса: ui / model / api / lib + index.ts)
Описание: «Модульность со строгими правилами зависимостей: слои сверху вниз,
публичный API слайса, проверка границ линтером».
Плюсы: высокая строгость границ (формальные слои + публичный API, проверка линтером); хорошая масштабируемость и предсказуемость; низкая связанность, лёгкий онбординг, постепенное внедрение.
Минусы: порог входа выше среднего — нужно освоить слои, слайсы и правила;
неоднозначность границ — размытая граница widgets/features, связанные сущности на одном слое;
хорошо ложится на React/Vue/Svelte, но с Angular сложнее — его структура конфликтует со слоями FSD.
Заголовок «Плюсы» — зелёным с иконкой плюса (✅), «Минусы» — красным с иконкой минуса (⛔).
Когда: средний и крупный SPA.
Визуал: вертикальная диаграмма слоёв со стрелками зависимостей вниз.
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```

## Слайд 9 — Уровень 4. Layered / Clean Architecture

```
Оформи текущий слайд на тему «Уровень 4. Layered / Clean Architecture».
Вводная: «Семейство подходов с общим принципом — Hexagonal (Cockburn),
Onion (Palermo), Clean (Martin)».
Принцип: «Бизнес-логика в центре и ничего не знает о UI, API, БД.
Зависимости направлены строго внутрь».
Размести две альтернативные схемы размещения слоёв рядом (моноширинный):
Вариант A — глобальные слои:
  src/
    domain/
    application/
    infrastructure/
    presentation/
  (домены внутри слоя: domain/order/, domain/user/)
Вариант B — слои внутри фичи (vertical slice):
  src/
    pages/home/
      domain/ application/
      infrastructure/ presentation/
    shared/  (те же слои, общие)
Подпись: «Оба варианта валидны; имена слоёв — одно из соглашений».
Плюсы: технологически независим — бизнес-логика изолирована от UI/фреймворка, легко заменить и переиспользовать (web + mobile); высокая тестируемость (ядро без UI и сети); высокая строгость границ (однонаправленные зависимости к центру).
Минусы: высокий порог входа — слои, порты/адаптеры, инверсия зависимостей; избыточен для простых приложений (домен вырождается в пустые типы); много кода и обёрток (маппинг между слоями).
Заголовок «Плюсы» — зелёным с иконкой плюса (✅), «Минусы» — красным с иконкой минуса (⛔).
Когда: тяжёлая клиентская логика, кроссплатформа, долгоживущий продукт.
Визуал: две схемы папок (A и B) расположи рядом двумя колонками; при наличии места сверху — концентрическая схема Clean Architecture (кольца + стрелка зависимостей внутрь).
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```

## Слайд 10 — Уровень 5. Monorepo + Package-based

```
Оформи текущий слайд на тему «Уровень 5. Monorepo + Package-based».
Подзаголовок: «Nx / Turborepo».
Размести структуру (моноширинный):
apps/   web/ admin/
libs/   feature-auth/ ui-kit/ domain-billing/
Описание: «Архитектура задаётся графом пакетов и правилами импортов между ними».
Плюсы: технологически независим — инструмент (Nx/Turbo) не привязан к фреймворку; высокая масштабируемость — переиспользование кода между приложениями, единые границы на уровне инструмента; быстрая сборка (кэш, инкрементальные билды).
Минусы: высокий порог входа — настройка инструментов и графа зависимостей; требует дисциплины версионирования и несёт риск «общего пакета на всё».
Заголовок «Плюсы» — зелёным с иконкой плюса (✅), «Минусы» — красным с иконкой минуса (⛔).
Когда: несколько приложений, общий код, большие команды.
Визуал: схема графа пакетов (apps и libs со связями).
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```

## Слайд 11 — Уровень 6. Micro-Frontends

```
Оформи текущий слайд на тему «Уровень 6. Micro-Frontends».
Размести структуру (моноширинный):
host/         (shell-приложение)
mfe-catalog/  (своя команда, свой деплой)
mfe-checkout/ (может быть другой стек)
shared-ui/
Описание: «Несколько фронтенд-приложений, каждое деплоится независимо.
Связь: Module Federation / Web Components / iframe».
Плюсы: максимальная технологическая независимость — каждое приложение на своём стеке; масштабируемость по командам — независимый деплой, автономия команд, изоляция отказов.
Минусы: очень высокий порог входа — оркестрация, общий рантайм, шаринг состояния между MFE нетривиален; операционная сложность — CI/CD на каждое приложение, риск рассинхрона UX.
Заголовок «Плюсы» — зелёным с иконкой плюса (✅), «Минусы» — красным с иконкой минуса (⛔).
Когда: 10+ команд, разные жизненные циклы частей продукта.
Визуал: схема host-приложения, объединяющего несколько независимых MFE-блоков.
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```

## Слайд 12 — Как выбрать подход

```
Оформи текущий слайд на тему «Как выбрать подход».
Размести таблицу «Масштаб → Базовый подход»:
— Маленькие (MVP, лендинг): Flat / Feature-based;
— Средние (один продукт, одна команда): Feature-based, Atomic, FSD;
— Крупные (большой продукт, домены): Atomic, FSD либо Clean;
— Из нескольких под-приложений: Monorepo / Micro-Frontends.
Ниже — блок «Подходы комбинируются»:
— Atomic организует UI, его сочетают с модульным/FSD для бизнес-логики;
— FSD уже использует луковичный принцип Clean (зависимости направлены вниз); Clean можно применять и точечно — для сложной доменной логики;
— Monorepo и Micro-Frontends — обёртки уровня системы.
Визуал: таблица сверху, ниже — короткий список про комбинирование.
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```

## Слайд 13 — Фактор 2026 и антипаттерны

```
Оформи текущий слайд на тему «Фактор AI и антипаттерны выбора».
Блок «Фактор AI (AI-assisted coding)»:
— строгая архитектура (FSD, Clean) стала ценнее: чёткие границы нужны и людям, и AI-агентам;
— без границ AI создаёт код разной гранулярности и ломает согласованность;
— договорённости важно фиксировать машиночитаемо — в линтере и в правилах AI-инструментов (Cursor rules, CLAUDE.md), тогда их соблюдают и люди, и AI.
Блок «Антипаттерны»:
— Micro-Frontends «на вырост» при одной команде;
— Clean Architecture в CRUD-лендинге;
— споры «atom или molecule» вместо доставки фич;
— FSD без линтера границ и культуры ревью.
Визуал: две колонки — слева фактор AI, справа список антипаттернов.
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```

## Слайд 14 — Итоги

```
Оформи текущий слайд как завершающий, на тему «Главные мысли».
Размести пронумерованный список:
1. Архитектура — не цель, а инструмент.
2. Любая архитектура лучше, чем её отсутствие.
3. Сложность архитектуры должна догонять боль проекта, а не опережать её.
4. Комбинировать и видоизменять подходы — нормально; главное — консистентность.
5. AI-эпоха повышает ценность строгих границ.
Внизу — строка «Q&A: какой уровень подходит нашим текущим проектам?».
Визуал: крупный нумерованный список, акцент на каждом тезисе.
Сохрани текущую визуальную тему слайда (фон, шрифты, цвета) без изменений.
```
